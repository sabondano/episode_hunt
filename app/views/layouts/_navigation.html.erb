<nav class="navbar navbar-default navbar-fixed-top navbar-dashboard">
  <div class="container">
    <%= image_tag "episode_hunt_logo_red.png", class: "logo" %>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <%= content_tag(:li, 
                        class: "#{'active' if current_page?(user_path(current_user))} #{'active' if current_page?(user_upvoted_path(current_user))}") do %>
                        <%= link_to user_path(current_user) do %>
                          <span class="glyphicon glyphicon-dashboard" aria-hidden="true"></span> My Profile
                        <% end %>
                      <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
            <span class="glyphicon glyphicon-option-horizontal" aria-hidden="true"></span>
          </a>
          <ul class="dropdown-menu">
            <li>
              <%= link_to "Suggest a podcast", new_podcast_path %>
            </li>
            <li>
              <%= link_to "All Podcasts", podcasts_path %>
            </li>
            <li>
              <%= link_to "Log out", logout_path %>
            </li>
          </ul>
        </li>
        <li>
          <a id="show-episode-submission-form-icon"><span class="glyphicon glyphicon-plus" aria-hidden="true"></span></a>
        </li>
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</nav>
<nav class="navbar navbar-default subnavbar">
  <div class="container">
    <ul class="nav navbar-nav">
      <%= content_tag(:li, class: "#{'active' if current_page?(episodes_path)}") do %>
        <%= link_to "All Episodes", episodes_path %>
      <% end %>
      <% @podcasts.each do |podcast| %>
        <%= content_tag(:li, class: "#{'active' if current_page?(podcast_path(podcast))}") do %>
          <%= link_to truncate(podcast.name, length: 15), podcast_path(podcast) %>
        <% end %>
      <% end %>
    </ul>
  </div>
</nav>
